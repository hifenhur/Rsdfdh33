<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.expark.pdvdesk" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SalesActivity"
            android:label="@string/title_activity_sells" >
        </activity>
        <activity
            android:name=".SaleActivity"
            android:label="@string/title_activity_sell" >
        </activity>
        <activity
            android:name="printer.PrinterActivity"
            android:label="PrinterActivity" >
        </activity>
        <activity
            android:name="printer.DeviceListActivity"
            android:label="DeviceListActivity" >
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
        </activity>
        <activity
            android:name=".CardSellActivity"
            android:label="@string/title_activity_card_sell" >
        </activity>
        <activity
            android:name=".CardRechargeActivity"
            android:label="@string/title_activity_card_recharge" >
        </activity>
    </application>

</manifest>
